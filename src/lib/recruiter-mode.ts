/** Nav item keys (see src/lib/navigation.ts) hidden or prioritized while Recruiter Mode is active. */
export const recruiterModeConfig = {
  hiddenRoutes: ["playground", "lab", "favorites", "reading", "changelog"],
  priorityRoutes: ["resume", "projects", "experience", "skills", "contact"],
};
